# Reproducibility scripts require:
duckdb>=1.0
# md_to_pdf.py (optional, renders papers to PDF) also needs:
markdown
# a few build/provenance scripts additionally use:
pandas
numpy
httpx
# config/ (published 2026-08-08 so scripts importing config.districts run from a bare clone):
pydantic
pydantic-settings
PyYAML