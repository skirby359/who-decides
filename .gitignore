data/
reports/*
!reports/.gitkeep
# Exception: a verification artifact a paper cites by name should ship with the paper.
# This is the district-level audit of the 54 backfilled Texas House seats referenced in
# safe-seat-washington.md Appendix F. Derived entirely from published election returns
# (district numbers, presidential vote totals, classification) — no PII.
!reports/tx_backfill_verification.csv
# The certified 2024 Texas House candidate list — the external source that verifies the
# backfill in Appendix F. Lives under data/ in the private repo, which is ignored here,
# so it ships from reports/. Published election returns; no PII.
!reports/2024_tx_house_candidates.csv
__pycache__/
*.pyc
.env
.DS_Store
docs/who-decides-washington.jargonheavy.pdf
